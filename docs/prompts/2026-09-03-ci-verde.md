**DOVE SI LANCIA: Claude Code** — nella cartella del repo `nc-movement`, su un ramo NUOVO `claude/ci-verde` creato da `main` (`834b0fd`, dopo il merge dello stacco).

**Task:** la CI di questo repo non è mai stata verde. Muore al passo `Lint` con 4 errori `no-explicit-any` in un solo file, e siccome `Lint` viene prima di `Test`, **i test in CI non sono mai stati eseguiti** — cancello anti-Lovable compreso. Qui si tolgono quei 4 `as any` sistemando i tipi, non nascondendoli, e si guarda la CI diventare verde e finalmente **eseguire i test**. Un file di codice, nient'altro.
**Data:** 2026-09-03
**Strumento di destinazione:** [x] Claude Code
**Branch previsto:** claude/ci-verde

## RITUALE D'APERTURA
Niente da provare. L'albero dovrebbe essere pulito: dopo lo stacco c'è `.gitattributes` con `* text=auto eol=lf` e `core.autocrlf=true` sul repo. Se `git status` non è pulito, dillo nel ritorno e fermati.

## LA MISURA (Cowork, 03/09, log della CI letto dal browser · `main` = `834b0fd`)
1. **Dove muore.** Run #34 sulla PR #1: `Set up job 2s · checkout 1s · Setup Bun 2s · Install 2s · **Lint 3s → fallito**`. Ultima riga: `✖ 21 problems (4 errors, 17 warnings)` poi `error: script "lint" exited with code 1`. **Il passo `Test` non è mai partito**, né in questo run né nei precedenti.
2. **Da quanto.** I run 28, 29, 30, 31, 32 e **33** (`af0f7d0`, la base dello stacco) sono tutti `failed`. La CI di questo repo **non è mai stata verde**.
3. **I 4 errori.** `src/pages/Dashboard.tsx:113-116`: `push(fms.data as any, 'FMS')`, e le tre sorelle `sfma`, `fcs`, `ybt`. Il file ha **0 righe di diff** nello stacco: gli errori sono più vecchi della migrazione.
4. **La firma che ricevono** (`Dashboard.tsx:106`): `const push = (rows: { client_id: string; assessed_at: string; total_score?: number | null }[] | null, kind: string)`.
5. **Una mia ipotesi CADUTA, dichiarata perché non ti porti dietro il mio errore.** Avevo pensato che i cast servissero perché `client_id` potesse essere nullo. **Falso, misurato sul database:** in tutte e quattro le tabelle `client_id` e `assessed_at` sono `NOT NULL`; solo `total_score` è annullabile, e la firma lo prevede già. **Quindi la vera causa non la so, e non la devi indovinare nemmeno tu: te la deve dire `tsc`.**
6. **I 17 warning restano.** Non sono nel perimetro: `bun run lint` esce 1 per gli errori, non per i warning.

## COSA FAI
1. **Togli i quattro `as any`** da `Dashboard.tsx:113-116` e lancia `bunx tsc --noEmit`. **Copia il messaggio esatto del compilatore nel ritorno**: è il fatto che questo prompt non ha.
2. **Sistema il disallineamento al livello dei tipi**, com'è giusto per ciascuno dei quattro. Le uniche tre strade ammesse: allargare o correggere la firma di `push`; tipizzare la `select` con la forma vera che PostgREST restituisce; oppure — se il compilatore rivela un caso che il codice oggi non gestisce (una riga che finirebbe come chiave nulla nella `Map`, un campo assente) — **gestirlo davvero**, e dirlo forte nel ritorno perché sarebbe un difetto vero, non di stile.
3. ⛔ **VIETATO nascondere:** nessun `as any` nuovo da nessuna parte, nessun `as unknown as`, nessun `// eslint-disable`, nessun `@ts-ignore`/`@ts-expect-error`, nessun allentamento di `tsconfig` o della configurazione eslint. Se l'unica via che vedi è una di queste, **fermati e scrivilo**: vuol dire che la fetta è più grande di così, e la rifacciamo.
4. **Non toccare il comportamento.** L'ordinamento dei clienti per ultima attività deve restare identico. Se il tuo tipo corretto cambia ciò che finisce nella `Map`, quello è il difetto del punto 2: nominalo, non passarci sopra in silenzio.
5. **Nient'altro.** I 17 warning non si sistemano, il workflow non si riscrive, `continue-on-error` sul type-check resta com'è.

## FILE
- **MODIFICATI:** `src/pages/Dashboard.tsx` · `docs/ULTIMO-RITORNO.md` · `docs/prompts/2026-09-03-ci-verde.md` (questo prompt, conservato).
- **VIETATI (zero righe di diff):** `.github/workflows/ci.yml` · `eslint.config.js` · `tsconfig*.json` · `src/integrations/**` · `src/test/**` · ogni altro file sotto `src/`.

## ACCEPTANCE (ognuno può bocciare)
1. `bun run lint` → **exit 0**, con l'output che mostra `0 errors` e i 17 warning ancora al loro posto (dichiara il numero: se cambia, spiega perché).
2. `bunx tsc --noEmit` exit 0 · `bun run test` exit 0 (5 file, 30 test) · `bun run build` exit 0.
3. `grep -rn "as any\|as unknown as\|eslint-disable\|@ts-ignore\|@ts-expect-error" src/pages/Dashboard.tsx` → **0 righe**.
4. 🔴 **Prova rossa, nelle due direzioni e con ripristino byte-identico:** rimetti **uno** dei quattro `as any`, lancia `bun run lint` → deve tornare **rosso con 1 error**; ripristina (md5 identico, `git status` pulito) → **verde**. Riporta i due output e i due md5. Serve a provare che il cancello del lint funziona davvero, non che il file oggi passa.
5. 🟢 **La prova che conta davvero, sulla CI vera:** apri la PR e **leggi il log del run**. Devono comparire, per la prima volta nella storia del repo, i passi `Lint` **verde** e `Test` **eseguito**, con dentro i 3 test di `cordone-lovable.test.ts`. Incolla nel ritorno le righe del log che lo mostrano. Se `Test` non compare, la fetta non è finita — anche se il lint è verde in locale.
6. `git diff --name-only origin/main...HEAD` = solo i tre FILE sopra; i VIETATI a 0 righe.

## COSA RIMANDI INDIETRO
`docs/ULTIMO-RITORNO.md` per questa fetta: ramo e hash · **il messaggio esatto di `tsc` dopo aver tolto i cast** (il punto 1) · quale delle tre strade hai preso e perché · se hai trovato un caso non gestito, che cos'era · l'acceptance voce per voce col comando e l'output · la prova rossa nei due stati con i md5 · **le righe del log della CI che mostrano `Test` eseguito** · il link della PR. Commit con `Co-Authored-By: Claude <noreply@anthropic.com>`.
